# Compatibility entry point for tools that require requirements.txt.
# Dependency versions and optional runtime groups are defined in pyproject.toml;
# prefer `uv sync` or install the required extras from that file.
-e .
