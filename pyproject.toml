[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "echoscript"
version = "0.2.0"
description = "Local Gradio audio/video transcription with pluggable ASR and speaker diarization"
authors = [{ name = "susuky" }]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = [
    "click>=8.1",
    "gradio>=6.0,<7",
    "opencc-python-reimplemented>=0.1.7",
    "qwen-asr>=0.0.6",
    "yt-dlp",
]
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

[project.optional-dependencies]
faster-whisper = ["faster-whisper>=1.1.0"]
diarization = ["pyannote-audio>=4.0,<5"]

[dependency-groups]
dev = [
    "pytest>=8",
    "pytest-cov>=5",
]

[project.urls]
Homepage = "https://github.com/susuky/echoscript"
Repository = "https://github.com/susuky/echoscript"

[project.scripts]
echoscript = "echoscript.cli:cli"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["test"]
addopts = "-q"
markers = [
    "integration: opt-in tests that run ffmpeg and real ASR/diarization models",
]
